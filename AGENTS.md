# BestLNG 长期协作规范

## 1. 项目定位

BestLNG 是一个开源、本地优先的语言学习项目，目标是帮助用户通过
“句子挖空 + 翻译提示 + 输入答案 + 错题复习”的方式学习外语。

第一阶段优先完成桌面客户端，让用户可以在本地开发态和本地安装环境中完成基础练习。
网页端第一阶段只作为发布页和下载入口；移动端、本地模型、云同步都作为后续扩展。

## 2. 推荐技术栈

- 包管理与仓库结构：pnpm workspace。
- 桌面客户端：Tauri 2、React、TypeScript、Vite。
- UI 与样式：Tailwind CSS，必要时引入成熟 React 组件库。
- 本地数据：SQLite。
- 发布页：Astro 或 Vite 静态站点。
- 版本发布与更新：GitHub Releases + Tauri updater。
- 后续云端：DigitalOcean 轻量服务，优先用于内容包索引、公告、反馈、版本信息等轻量能力。
- 后续本地模型：预留 Provider 接口，优先支持用户自行配置本地推理服务。

## 3. 长期任务与日志文件

- 长期任务清单：`docs/TASK_PLAN.md`
- 阶段开发日志：`docs/DEVELOPMENT_LOG.md`

每次开始开发前，必须先读取：

1. `AGENTS.md`
2. `docs/TASK_PLAN.md`
3. `docs/DEVELOPMENT_LOG.md`
4. 当前任务相关的源码、配置和文档

## 4. 任务执行规则

- 必须从 `docs/TASK_PLAN.md` 中第一个未完成的 `- [ ]` 任务继续执行。
- 除非用户明确调整优先级，不要跳过前面的未完成任务。
- 每完成一个任务，必须把对应清单项从 `- [ ]` 更新为 `- [x]`。
- 如果一个任务太大，需要先拆成更小的子任务，再开始实现。
- 如果发现清单与当前项目状态不一致，先修正清单和日志，再继续开发。
- 不允许删除历史任务记录；需要变更方向时，追加说明或新增任务。

## 5. 开发日志规则

每完成一次开发，都必须更新 `docs/DEVELOPMENT_LOG.md`。

更新日志前必须先读取整个日志文件，确认最后一个日志序号，然后在文件末尾追加新的日志条目。
日志只能追加，不能覆盖、重写或删除之前阶段的记录。

每条日志至少包含：

- 本阶段完成了什么。
- 修改或新增了哪些文件。
- 运行了哪些验证命令。
- 如果没有运行 lint、typecheck 或 test，必须说明原因。
- 遗留问题、下一步建议或风险。

日志序号必须按 `1、2、3...` 递增。

## 6. 编码要求

- 所有对话、说明和开发总结默认使用中文。
- 关键业务逻辑、复杂流程、数据迁移和公共 API 必须写中文注释。
- 公共函数、公共类型、模块入口需要写清楚用途、参数和返回值。
- 优先复用已有实现，避免重复代码。
- 遵循 DRY、KISS、YAGNI 原则，不做过早抽象。
- 保持最小改动，只修改完成当前任务所必需的文件。
- 严格使用 TypeScript 类型，禁止无处理的 `any` 或 `unknown`。
- 不硬编码密钥、Token、密码、私有域名配置或其它敏感信息。
- 所有外部内容包必须记录来源和许可证，避免打包版权不明确的数据。

## 7. 验证要求

每次代码变更后，必须按项目实际配置运行验证命令。项目初始化后，默认至少包括：

- `pnpm lint`
- `pnpm typecheck`
- `pnpm test`
- 如涉及 Tauri/Rust 代码，额外运行 `cargo test`

如果项目尚未配置对应命令，或本次只修改文档，需要在开发日志和最终回复中说明未运行的原因。

## 8. Git 与文件安全

- 不要覆盖用户未明确要求修改的文件。
- 不要使用 `git reset --hard`、`git checkout --` 等破坏性命令，除非用户明确要求。
- 工作区出现他人或用户已有改动时，先理解改动再继续，不能直接回滚。
- 新增文件应优先放在既定目录中，避免随意散落。

## 9. 后续 AI 接续要求

后续任何 AI 或开发者接手时，都必须先确认长期任务清单和开发日志。
如果用户没有指定新任务，应从 `docs/TASK_PLAN.md` 中第一个未完成项继续。
完成任务后，必须同步更新任务清单和开发日志，确保项目可以长期接续推进。
